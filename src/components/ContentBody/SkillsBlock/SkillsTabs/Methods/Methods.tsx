import React from 'react'
import { CardContent } from '../CardContent'

export const Methods: React.FC = () => {
	return (
		<CardContent
			leftContent={<div>not the same, need to think</div>}
			cardContent={<div>card</div>}
		/>
	)
}
